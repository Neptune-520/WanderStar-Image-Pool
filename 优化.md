项目主内容已经全部完成，接下来来做细节优化

1。后台：
1.1.去除左上角Admin区块，<span class="badge">Admin</span>

1.2.分类页-分类概念全部替换为“端点”
1.2.1去除小字“管理该分类下的所有 API”
1.2.2.去除“分类：”，直接展示分类名，在分类名后面展示实际api链接并可点击跳转
1.2.3.右上角新增“编辑分类”按钮
1.2.4.每个分类右上角新增两个按钮：清除该分类下的自动熔断、清除该分类下的手动封禁
1.2.5.每个分类下的api都可以编辑
1.2.6.自动熔断后的恢复按钮移动至右侧手动封禁开关左侧
1.2.7.新增字段：端点描述，前台也要展示，位置：“调用该接口获取随机图片”字样处

1.3.分类功能：目前分类名和url是一个输入框，要变成两个输入框，分类名（可中文）、分类url（仅英文）

1.4.菜单
1.4.1.菜单上的分类要有两个快捷按钮，编辑和删除
1.4.2.查看对外主页字样修改为查看前台

1.5.系统设置
1.5.1.公开域名设置，去掉“设置对外展示的域名，留空则自动获取请求主机的 IP 或域名。示例: https://api.mydomain.com”，输入框默认填入自动获取的主机或ip地址
1.5.2.管理员目前只需要密码登录，增加用户名，方便后续拓展用户系统
1.5.3.基础网站配置：网站名称、logo，主页显示文本内容，公告弹窗，底部信息，背景设置（背景需要支持纯色背景，渐变背景，图片背景，要有颜色选择器）
1.5.4.去除右上角“配置全局参数与管理员权限”字样
1.5.5.增加“联系”功能配置，可上传图片和展示文字，支持社交媒体链接等，展示在前台联系页中
1.5.6.api请求次数和请求时间设置，对应3.1优化

2.前台
2.1.去除顶部管理后台按钮
2.2.顶部增加菜单，首页、api、联系
2.3.首页仅展示三个api端点，底部增加按钮“查看更多api”
2.4.api页面展示所有端点
2.5.联系展示后台设置后的联系内容

3.系统优化
3.1.调用机制优化，目前如果一个api调用失败时控制台会报大量的错
“
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
[Router] API 请求失败 - 1: timeout of 5000ms exceeded
”
优化为一个api只请求两次，两次都失败或超过1.5秒则自动选择下一个api（默认次数两次，默认时间1.5s），这个时间和次数可设置，，并除了手动封禁和自动熔断外，新增一个状态为访问超时
